The table below has been updated to include your requested datasets. No duplicate links have been introduced, and each entry details the domain, core schema fields, and conceptual alignment with the MM-PEFT-ASD architecture.

| Dataset Name & URL | Domain | Core Schema Fields | Conceptual Mapping to MM-PEFT-ASD Architecture |
| :--- | :--- | :--- | :--- |
| **Real Estate Search**<br>[Binaryy/multimodal-real-estate-search](https://huggingface.co/datasets/Binaryy/multimodal-real-estate-search) | Real Estate | <ul><li>`images` (List of property photos)</li><li>`description` (Text summary)</li><li>`location` (Text location details)</li><li>`details` (Structural/feature list)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Processes the listing's interior/exterior photos (mapping directly to the multi-axis axial, coronal, and sagittal view inputs).</li><li>**Metadata (Patient Info):** Tabular `location` and text `description` are formatted into the system prompt.</li><li>**LLM + LoRA Output:** Estimates property value brackets (severity estimation) and generates selling points or warning highlights (precautionary recommendations).</li></ul> |
| **DeepFashion Multimodal**<br>[Marqo/deepfashion-multimodal](https://huggingface.co/datasets/Marqo/deepfashion-multimodal) | E-Commerce / Fashion | <ul><li>`image` (PIL Image)</li><li>`description` / `captions` (Text describing colors, textures, cuts, and categories)</li><li>`metadata` (Attribute category labels)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Encodes clothing photos from different catalog views.</li><li>**Metadata (Patient Info):** The model merges the tabular category/material metadata into a single string.</li><li>**LLM + LoRA Output:** Generates rich product catalog details or performs structural attribute labeling (classification task).</li></ul> |
| **OBELICS**<br>[HuggingFaceM4/OBELICS](https://huggingface.co/datasets/HuggingFaceM4/OBELICS) | General Web Documents | <ul><li>`images` (List of image files/URLs)</li><li>`texts` (List of text segments)</li><li>`metadata` (Source webpage data)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Handles sequences of images in interleaved positions, mimicking multi-image patient inputs.</li><li>**Metadata (Patient Info):** The text segments surrounding the images serve as the prompt context.</li><li>**LLM + LoRA Output:** Predicts the subsequent text tokens based on sequential visual-textual context (mimicking multimodal context-aware decision generation).</li></ul> |
| **Multimodal Open R1**<br>[lmms-lab/multimodal-open-r1-8k-verified](https://huggingface.co/datasets/lmms-lab/multimodal-open-r1-8k-verified) | Mathematics & Geometry Reasoning | <ul><li>`image` (Problem diagram)</li><li>`problem` (Text question)</li><li>`reasoning` / `thinking_process` (Text string enclosed in `<think>...</think>` tags)</li><li>`answer` (Final target choice/number)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Ingests the problem diagram or visual context.</li><li>**Metadata (Patient Info):** The `problem` text functions as the clinical prompt.</li><li>**LLM + LoRA Output:** Instead of direct classification, the model generates an explicit reasoning trace first, followed by the final decision. This is highly applicable for clinical applications where visual diagnosis requires explanatory intermediate thinking before yielding a final classification.</li></ul> |
| **Docmatix**<br>[HuggingFaceM4/Docmatix](https://huggingface.co/datasets/HuggingFaceM4/Docmatix) | Document Visual QA (DocVQA) | <ul><li>`images` (List of multi-page document images)</li><li>`texts` (Conversational list format with `user` queries, `assistant` responses, and `source` markers)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Maps multiple document pages (replacing 3D MRI cross-sections) to visual tokens.</li><li>**Metadata (Patient Info):** The `user` queries act as user instructions or system constraints.</li><li>**LLM + LoRA Output:** Generates structured textual extractions or QA validations (assistant responses) directly conditioned on visual document evidence.</li></ul> |
| **TextCaps**<br>[textcaps](https://textvqa.org/textcaps/download/) | Scene Text Captioning | <ul><li>`image` (PIL Image)</li><li>`image_id` (String identifier)</li><li>`captions` (List of textual captions requiring scene text parsing)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Captures background textures alongside structural written text embedded within the scene.</li><li>**Metadata (Patient Info):** System prompt acts as an instruction to identify and describe physical text in the frame.</li><li>**LLM + LoRA Output:** Generates detailed text narratives that synthesize both visual objects and readable text tokens in the environment.</li></ul> |
| **LLaVA Instruct Mix VSFT**<br>[HuggingFaceH4/llava-instruct-mix-vsft](https://huggingface.co/datasets/HuggingFaceH4/llava-instruct-mix-vsft) | General Chat & Instruction Tuning | <ul><li>`images` (List of PIL Images)</li><li>`messages` (Conversational array containing `role` and `content` fields)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Encodes context images and aligns their patch coordinates with the text projection matrix.</li><li>**Metadata (Patient Info):** Supports multi-turn conversation context history as textual input sequence.</li><li>**LLM + LoRA Output:** Fine-tunes conversational generation to act as a general-purpose vision assistant, mimicking the iterative clinical QA process.</li></ul> |
| **The Cauldron**<br>[HuggingFaceM4/the_cauldron](https://huggingface.co/datasets/HuggingFaceM4/the_cauldron) | Multitask VLM Finetuning (50 Subsets) | <ul><li>`images` (Sequence of PIL Images)</li><li>`texts` (Conversational list with `user` and `assistant` keys)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Adapts to highly diverse spatial structures (diagrams, textbooks, charts, screenshots).</li><li>**Metadata (Patient Info):** Translates custom instructions and task definitions nested inside the `user` prompts.</li><li>**LLM + LoRA Output:** Optimizes task-switching parameter-efficiency, allowing a single unified adapter to generate actions, text, or structured answers.</li></ul> |
| **DocVQA**<br>[lmms-lab/DocVQA](https://huggingface.co/datasets/lmms-lab/DocVQA) | OCR-Based Document QA | <ul><li>`image` (PIL Image of page/scanned form)</li><li>`question` (Text query)</li><li>`answers` (List of target strings)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Processes scanned layout, tabular borders, and printed/handwritten characters.</li><li>**Metadata (Patient Info):** The analytical question serves as the textual conditional prompt.</li><li>**LLM + LoRA Output:** Localizes and extracts accurate word-level text directly from the structural layout coordinates.</li></ul> |
| **ChartQA**<br>[HuggingFaceM4/ChartQA](https://huggingface.co/datasets/HuggingFaceM4/ChartQA) | Visual Chart & Plot Reasoning | <ul><li>`image` (Plot or Chart image)</li><li>`query` / `question` (Text query)</li><li>`label` / `answer` (Correct numerical value or status)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Identifies data trends, axes, line charts, bar plots, and legends.</li><li>**Metadata (Patient Info):** Integrates quantitative queries into the prompt pipeline.</li><li>**LLM + LoRA Output:** Estimates quantities, performs logical comparisons, and outputs numeric predictions (mapping directly to severity estimation tasks).</li></ul> |
| **TextVQA**<br>[lmms-lab/textvqa](https://huggingface.co/datasets/lmms-lab/textvqa) | Visual Scene OCR QA | <ul><li>`image` (PIL Image)</li><li>`question` (Text query asking to read scene characters)</li><li>`answers` (List of ground truth strings)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Focuses on detecting skewed, shadowed, or stylized fonts on real-world objects.</li><li>**Metadata (Patient Info):** Employs scene-text queries as prompt strings.</li><li>**LLM + LoRA Output:** Decodes text elements to provide direct, concise answers about the visual scene.</li></ul> |
| **AI2D**<br>[lmms-lab/ai2d](https://huggingface.co/datasets/lmms-lab/ai2d) | Diagrammatic Science QA | <ul><li>`image` (Diagram illustration)</li><li>`question` (Conceptual science query)</li><li>`options` (Multiple choice candidates)</li><li>`answer` (Correct choice index or string)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Analyzes textbook-style illustrations, tracking flow arrows, relational nodes, and labels.</li><li>**Metadata (Patient Info):** Merges the science question and the discrete choice options into the text prompt.</li><li>**LLM + LoRA Output:** Chooses the correct multiple-choice index (categorical classification similar to classifying ASD/Non-ASD).</li></ul> |
| **LLaVA NeXT Data**<br>[lmms-lab/LLaVA-NeXT-Data](https://huggingface.co/datasets/lmms-lab/LLaVA-NeXT-Data) | Multi-Resolution Instruction Tuning | <ul><li>`id` (Unique string)</li><li>`image` (High-res PIL Image or local path)</li><li>`conversations` (Turns with `from` and `value` fields)</li></ul> | <ul><li>**Vision Encoder & Linear Projection:** Handles high-resolution inputs by utilizing adaptive image splitting (chopping high-res visual structures into grid tokens).</li><li>**Metadata (Patient Info):** Embeds structured `human` chat turns to condition the generation context.</li><li>**LLM + LoRA Output:** Minimizes hallucination rates during the generation of complex visual descriptions (`gpt` values).</li></ul> |
